# Setpoint for the attitude of the drone

Header header

# Attitude setpoint
float64 roll # in deg, positive value will roll right
float64 pitch # in deg, positive value will pitch up
float64 yaw_rate # in deg/s

# Throttle setpoint
float64 climb_rate # in m/s vertical speed (negative is down)